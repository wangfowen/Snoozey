<h1>Listing shames</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shames.each do |shame| %>
  <tr>
    <td><%= link_to 'Show', shame %></td>
    <td><%= link_to 'Edit', edit_shame_path(shame) %></td>
    <td><%= link_to 'Destroy', shame, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shame', new_shame_path %>
